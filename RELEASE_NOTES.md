# Analiz Operacii — заметки к релизу

## 1.1.7
- Исправление: добавление неизвестных операций вставляет строку так, что перед «Всего операций» сохраняются 2 пустые строки
- Разделитель: между «Дети всего» и «Человек» поддерживается 1 пустая строка (с учётом сдвигов строк)
- Flet: вкладка «Не классифицировано» — можно назначить существующую категорию (ручная отметка)
- Flet: вкладка «Спорные» — в диалоге отображаются название операции и список кандидатов
- SummaryWriter: запись счётчиков «Дети/Человек» привязана к фактическим строкам меток в листе

## 1.1.6
- Исправление: добавление категории из «Не классифицировано» снова вставляет строку в сводную Excel (раньше сдвигался только config — таблица «съезжала»)
- При ошибке записи в Excel категория откатывается из config

## 1.1.5
- Проверка обновлений в Flet: кнопка «Обновления…» в верхней панели
- Прогресс: скачивание (МБ), проверка SHA-256, распаковка и установка файлов
- После установки — предложение перезапустить приложение

## 1.1.4
- Конструктор классификации во вкладках «Не классифицировано» и «Спорные»: добавить категорию в config / назначить вручную перед записью в Excel
- Предупреждение на панели и счётчики в сайдбаре, если есть неклассифицированные или спорные операции

## 1.1.3
- Иконка в окне и панели задач: патч `flet.exe` при сборке (раньше менялся только ярлык `AnalizOperacii.exe`)
- Мультиразмерный `.ico` (16–256), клиент Flet из комплекта сборки (`FLET_VIEW_PATH`), без стандартной иконки из `~/.flet`

## 1.1.2
- Windows-сборка на Flet UI (`run_flet.py`), пакет `flet` / `flet-desktop` / `ui_flet`
- Иконка приложения (скальпель и ножницы) в exe
- Исправление ложного «файл занят»: `~$…xlsx` после сбоя Excel больше не блокирует запись
- Обновление: повтор копирования и замена занятого `.exe` через `.old`

## 1.1.1
- Вкладка «Проверки»: длительные операции (порог `long_op_hours`, по умолчанию >4 ч) и отсутствие опер. стола
- Вывод: № истории, ФИО пациента, хирург, операция; экспорт в Excel
- Сборка Windows: все модули `analyzers`, схемы, `RELEASE_NOTES.md`, `form14_overrides.yaml`

## 1.1.0
- Новый UI на Flet (`run_flet.py`): боковая навигация, превью таблиц, KPI на вкладке «Работа»
- Конструктор формы № 14: назначение/отвязка строк, фильтр по отделению и строке формы, морфология
- Превью формы № 14 для хирургии/травмы из операций отделения (не шаблон ЛОР 4001)
- ЛОР: превью формы 4001; калибровка ЛОР не подмешивается в другие отделения
- Overrides `form14_overrides.yaml`, импорт/экспорт Excel; защита от Excel-формул в комментариях
- Журнал анализа: лимит 500 строк, автопрокрутка к концу
- Схемы UI/конфига, расширенные тесты по всем отделениям

## 1.0.5
- Сводные для отделений (1/2 хирургия, детская хирургия, травматология): свой рубрикатор (1 код = 1 строка), Excel без формы 4001
- Создание сводной отделения из UI; add/delete операций для активного отделения
- План/экстр по ЭМК: классификация категорий, спорные в Excel для ручной сверки; загрузчик отчёта «Заполнение ЭМК в стационаре»
- Улучшенная привязка КВС к типу госпитализации (±1 день / ближайший эпизод)
- Оформление сводной как у ЛОР: салат/оранж (тема Excel), заголовок в B1, «Неделя с/по» вправо, формулы итогов, заливка столбца сумм
- Инвентаризация кодов отделения + сопоставление с KSGoperacii.csv

## 1.0.4
- Окно «Что нового» после обновления и при первом запуске новой версии (Помощь → Что нового…)
- Добавление/удаление операций через UI: вставка и удаление строк в Excel, ОБЩАЯ и графики
- Между списком операций и «Всего операций» всегда одна пустая строка-разделитель
- Цвета план/экстр: тема Excel акцент 6 / акцент 2, светлый 80%; заливка и границы B–H
- В сводной нули по операциям не пишутся — ячейка остаётся пустой
- Вкладка «Спорные»: ничьи по ключевым словам + ручное назначение категории; правка ключей категорий
- Шапка в два ряда: зелёные основные кнопки, оранжевые ЭМК; добавление/удаление операций в шапке; светлая/тёмная тема
- Бэкапы сводной в папку `backups/` (до 20 шт.)
- Поиск в рубрикаторе КСГ: кнопка «Поиск» и Enter; выбор обновляет название

## 1.0.3
- Автопроверка обновлений при старте не чаще раза в N дней (`updates.check_interval_days`)
- Вкладка «Расхождения ЭМК» — таблица + экспорт
- Отделения: подсказка профиля, путь сводной по отделению, предупреждение при смене/записи
- Экспорт проблемных кодов + черновик фрагментов для `config.yaml`

## 1.0.2
- Сводная в любой папке (путь + «Обзор…»)
- Кнопка «Сводная на год…» в настройках
- Исправлена дата C2 при создании года (формат ДД/ММ/ГГГГ, без времени)
- Очистка колонки S формы 4001 («Всего из гр.3 т.4000») при новом годе

## 1.0.1
- Сводная операций, форма 4001, превью
- Обновления с GitHub
- Сборка Windows: папка `AnalizOperacii/` (не один файл)
